"""Stubs / mocks track — parallel to the data track.

Generates API stubs (WireMock, Pact, Postman, OpenAPI examples, JSON/XML
fixtures) from `MockConfig` documents (`sdp-mock-v1`). Independent of
`TableConfig` / `RelationshipConfig` and the data-generation core.

See `Stubs_Mocks_Plan.md` for the design rationale.
"""

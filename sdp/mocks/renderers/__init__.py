"""Output renderers for the stubs/mocks track.

Each renderer takes a `MockConfig` and writes one or more files to a target
directory. They share the `TemplateEngine` so generated values are
consistent across renderer formats.
"""
